#include "main.h"

/**
   *_abs - computes the absolute value of a number
   *@n: The num to be computed
   *
   *Return: This function computed +ve int number
   */

int _abs(int n)
{
if (n >= 0)
{
return (n);
}
else
{
return (n * -1);
}
}
